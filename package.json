{
  "name": "threads-backup-cli",
  "version": "0.1.0",
  "description": "Back up your Threads posts and media with the official Threads API.",
  "type": "module",
  "bin": {
    "threadsBackup": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "keywords": [
    "threads",
    "backup",
    "cli"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
